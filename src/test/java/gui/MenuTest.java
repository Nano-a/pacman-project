/*mport static org.junit.Assert.*;
import java.util.logging.*;

public class MenuTest {
    private static final Logger LOGGER = Logger.getLogger(MenuTest.class.getName());

    @Test
    public void testGetJouerButton() {
        Menu menu = new Menu();
        Button jouerButton = menu.getJouerButton();
        assertNotNull(jouerButton);
        assertEquals("Jouer", jouerButton.getText());
        LOGGER.log(Level.INFO, "Jouer button text is: " + jouerButton.getText());
    }

    @Test
    public void testGetParametresButton() {
        Menu menu = new Menu();
        Button parametresButton = menu.getParametresButton();
        assertNotNull(parametresButton);
        assertEquals("Paramètres", parametresButton.getText());
        LOGGER.log(Level.INFO, "Parametres button text is: " + parametresButton.getText());
    }

    @Test
    public void testGetQuitterButton() {
        Menu menu = new Menu();
        Button quitterButton = menu.getQuitterButton();
        assertNotNull(quitterButton);
        assertEquals("Quitter", quitterButton.getText());
        LOGGER.log(Level.INFO, "Quitter button text is: " + quitterButton.getText());
    }
}*/